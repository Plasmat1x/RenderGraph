## 🎯 Цель сценария
Разработчик игры создает эффективный pipeline рендеринга для 3D сцены с post-processing эффектами, используя Render Graph систему.

## 👤 Актеры
- Первичный актер: Game Developer (разработчик игры)
- Вторичные актеры: Graphics Device, ECS System, Asset Pipeline

## 📋 Предусловия
- ✅ Graphics Device инициализировано (DirectX 12 / Vulkan)
- ✅ ECS система настроена с компонентами Transform, MeshRenderer, Camera
- ✅ Asset Pipeline загрузил меши, текстуры и шейдеры
- ✅ Render Graph библиотека подключена к проекту

## 🎬 Основной поток выполнения

Фаза 1: Инициализация системы
1.1 Разработчик создает Render Graph, передавая инициализированное Graphics Device
- Система автоматически создает ResourceManager для управления GPU ресурсами
- Инициализируется DependencyResolver для анализа зависимостей между проходами
- Создается RenderGraphBuilder для декларативного описания ресурсов

1.2 Система готова к добавлению render passes
Фаза 2: Создание Render Passes
2.1 Разработчик создает GeometryPass для рендеринга 3D геометрии
- Настраивает параметры: разрешение (1920x1080), цвет очистки (темно-синий)
- Подготавливает список объектов для рендеринга из ECS компонентов

2.2 Разработчик создает BlurPass для размытия изображения
- Устанавливает параметры размытия: радиус = 8 пикселей, качество = High
- Выбирает двухпроходное размытие (горизонтальное + вертикальное)

2.3 Разработчик создает ColorCorrectionPass для финальной обработки
- Настраивает цветокоррекцию: гамма = 2.2, контраст = 1.1, яркость = 0.1
- Включает ACES tone mapping для HDR контента

Фаза 3: Настройка связей между проходами
3.1 Разработчик соединяет выход GeometryPass с входом BlurPass
- BlurPass.InputTexture = GeometryPass.ColorTarget
- Система автоматически определяет зависимость между проходами

3.2 Разработчик соединяет выход BlurPass с входом ColorCorrectionPass
- ColorCorrectionPass.InputTexture = BlurPass.OutputTexture
- Формируется цепочка: Geometry → Blur → ColorCorrection

Фаза 4: Добавление проходов в граф
4.1 Разработчик добавляет все проходы в Render Graph
- Система регистрирует каждый проход и создает для него контекст выполнения
- Каждому проходу назначается категория (Rendering, PostProcessing) и приоритет

4.2 Граф содержит 3 прохода, готовых к компиляции
Фаза 5: Компиляция графа
5.1 Разработчик запускает компиляцию графа
- Система начинает фазу Setup для каждого прохода

5.2 GeometryPass создает свои ресурсы:
- Color target (1920x1080, RGBA8) для рендеринга цвета
- Depth target (1920x1080, D32_FLOAT) для буфера глубины
- Constant buffer (256 байт) для матриц камеры
- Регистрирует эти ресурсы как выходы прохода

5.3 BlurPass анализирует входную текстуру и создает выходную:
- Читает цветовую текстуру от GeometryPass
- Создает выходную текстуру того же формата и размера
- Создает промежуточную текстуру для двухпроходного размытия
- Создает constant buffer для параметров размытия

5.4 ColorCorrectionPass завершает цепочку:
- Читает размытую текстуру от BlurPass
- Создает финальную выходную текстуру
- Создает constant buffer для параметров цветокоррекции

5.5 DependencyResolver анализирует зависимости:
- Строит направленный граф зависимостей между проходами
- Проверяет отсутствие циклических зависимостей
- Выполняет топологическую сортировку
- Определяет порядок выполнения: GeometryPass → BlurPass → ColorCorrectionPass

5.6 Система проводит валидацию:
- Проверяет корректность всех resource handles
- Анализирует конфликты использования ресурсов
- Подтверждает совместимость форматов и размеров

5.7 ResourceManager оптимизирует использование ресурсов:
- Настраивает пулинг для переиспользования промежуточных ресурсов
- Анализирует возможности алиасинга памяти
- Подготавливает систему управления lifetime'ом ресурсов

5.8 Каждый проход получает уведомление OnGraphCompiled:
- GeometryPass создает vertex и pixel шейдеры для рендеринга меша
- BlurPass создает специализированные шейдеры для горизонтального и вертикального размытия
- ColorCorrectionPass создает шейдер с поддержкой различных tone mapping алгоритмов
- Все проходы создают необходимые семплеры (linear, point, anisotropic)

5.9 Компиляция завершена успешно, граф готов к выполнению
Фаза 6: Выполнение рендер лупа
6.1 Каждый кадр разработчик обновляет данные сцены:
- Обновляет позицию и матрицы камеры
- Передает время кадра (deltaTime = 16.67ms для 60 FPS)
- Устанавливает размеры экрана (1920x1080)

6.2 Разработчик создает Command Buffer и запускает выполнение графа
6.3 Система выполняет GeometryPass:
- Устанавливает color и depth targets
- Очищает targets заданными цветами
- Обновляет constant buffer с матрицами камеры
- Для каждого объекта в сцене:
    - Устанавливает vertex и index буферы меша
    - Привязывает материал (шейдеры, текстуры, семплеры)
    - Отправляет DrawIndexed команду
- Записывает статистику выполнения (время: 2.1ms, 347 draw calls)

6.4 Система автоматически переводит color target в состояние Shader Resource
6.5 Система выполняет BlurPass:

- Привязывает цветовую текстуру от GeometryPass как входную

- Горизонтальный проход:
    - Устанавливает промежуточную текстуру как render target
    - Привязывает горизонтальный blur шейдер
    - Рендерит fullscreen quad для применения эффекта
- Вертикальный проход:
    - Использует промежуточную текстуру как вход
    - Устанавливает финальную выходную текстуру как render target
    - Применяет вертикальное размытие
- Записывает статистику (время: 1.2ms, 2 draw calls)

6.6 Система выполняет ColorCorrectionPass:
- Привязывает размытую текстуру как входную
- Обновляет constant buffer параметрами цветокоррекции
- Применяет цветокоррекцию и tone mapping за один проход
- Выводит финальное изображение в output текстуру
- Записывает статистику (время: 0.8ms, 1 draw call)

6.7 Command Buffer отправляется на GPU для выполнения
6.8 Система собирает статистику кадра:
- Общее время рендеринга: 4.1ms
- Использование памяти: 31.7MB (текстуры: 31.7MB, буферы: 64KB)
- Эффективность проходов: 100% (все проходы выполнены)
- Статистика Draw calls: 350 total

Фаза 7: Мониторинг и оптимизация
7.1 Разработчик анализирует статистику производительности:
- GeometryPass занимает 51% времени рендеринга (самый затратный)
- BlurPass и ColorCorrectionPass эффективны
- Использование памяти стабильно, фрагментация минимальна

7.2 При необходимости разработчик может:
- Отключить отдельные проходы для тестирования
- Изменить параметры эффектов в реальном времени
- Добавить новые проходы без пересборки всего pipeline
- Переставить проходы местами для экспериментов

7.3 Система автоматически оптимизирует:
- Переиспользует ресурсы между кадрами через пулинг
- Минимизирует переходы состояний ресурсов
- Кэширует compiled шейдеры для быстрой загрузки

## ✅ Постусловия успешного выполнения

- 🎨 Кадр отрендерен: Финальное изображение содержит 3D сцену с примененным размытием и цветокоррекцией
- 📊 Статистика собрана: Доступна детальная информация о производительности каждого прохода
- 💾 Ресурсы оптимизированы: Память используется эффективно, нет утечек
- 🔄 Система готова к следующему кадру: Все состояния сброшены, пулы ресурсов готовы
- 📈 Performance target достигнут: Кадр рендерится за 4.1ms (< 16.67ms для 60 FPS)


## 🎉 Результат для разработчика
Разработчик получил мощную и гибкую систему рендеринга, которая:

- ✨ Автоматически управляет сложными зависимостями между проходами
- 🚀 Оптимизирует производительность без ручного вмешательства
- 🔧 Легко расширяется новыми эффектами и техниками
- 📊 Предоставляет детальную аналитику для профилирования
- 🎯 Абстрагирует сложность GPU программирования
- 🔄 Масштабируется от простых 2D игр до AAA проектов

## 📝 Примечания
- Время выполнения: Полный цикл от создания до первого кадра занимает ~50ms
- Память: Пиковое использование 32MB для FullHD рендеринга
- Масштабируемость: Легко добавить shadow mapping, SSAO, или другие эффекты
- Совместимость: Одинаково работает на DirectX 12, Vulkan, и других API

