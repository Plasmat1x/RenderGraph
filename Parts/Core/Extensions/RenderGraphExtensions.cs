using GraphicsAPI.Interfaces;

namespace Core;

public static class RenderGraphExtensions
{
  // ????? ??? ????????? graphics device (????? ???????? ? RenderGraph)
  public static IGraphicsDevice GetGraphicsDevice(this RenderGraph renderGraph)
  {
    // ? ???????? ?????????? RenderGraph ?????? ????????????? ?????? ? device
    // ??? ???? ????? ??????? null ? ???????????? ??? ? ????????
    return null;
  }
}